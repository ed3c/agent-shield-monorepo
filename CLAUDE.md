# CLAUDE.md — Agent Shield Claude Code entry

Read `AGENTS.md`, `ARCHITECTURE.md`, the selected module contract, and `data/status/integration.json` before changing code.

Use the same typed module boundaries as Codex. Do not replace a missing provider or live receipt with an inferred PASS. Project MCP approval, browser-session ownership, secrets, Human Admit, promotion, and rollback remain human-owned.
