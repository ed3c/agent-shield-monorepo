name: Phase 3 hybrid exchange contracts

on:
  pull_request:
    paths:
      - "services/runtime-fabric/src/exchange/**"
      - ".github/workflows/p3-hybrid-exchange.yml"
  push:
    branches: [main]
    paths:
      - "services/runtime-fabric/src/exchange/**"
      - ".github/workflows/p3-hybrid-exchange.yml"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - name: Compile exchange entrypoint
        run: bun build services/runtime-fabric/src/exchange/selftest.ts --target=bun --outdir=/tmp/rt-xchg-build
      - name: Run exchange positive and disagreement controls
        run: bun services/runtime-fabric/src/exchange/selftest.ts
      - name: Typecheck repository
        run: bunx tsc --noEmit
