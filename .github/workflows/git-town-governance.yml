name: Git Town governance

on:
  pull_request:
    paths:
      - ".git-town.toml"
      - "CONTRIBUTING.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/PULL_REQUEST_TEMPLATE.md"
      - ".github/workflows/git-town-governance.yml"
      - "docs/git/**"
      - "scripts/git-town/**"
      - "third_party/git-town/**"
  push:
    branches:
      - main
    paths:
      - ".git-town.toml"
      - "CONTRIBUTING.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/PULL_REQUEST_TEMPLATE.md"
      - ".github/workflows/git-town-governance.yml"
      - "docs/git/**"
      - "scripts/git-town/**"
      - "third_party/git-town/**"

permissions:
  contents: read

jobs:
  static-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Bash and fail-closed Git Town policy
        shell: bash
        run: bash scripts/git-town/selftest.sh

  live-integration-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Preserve runtime evidence boundary
        shell: bash
        run: |
          printf '%s\n' 'Git Town 24.0.0 Linux executable integration: ABSENT'
          printf '%s\n' 'Reason: issue #31 admitted one exact macOS arm64 host artifact; no exact Linux artifact or execution environment is admitted.'
          printf '%s\n' 'Static contract success cannot substitute for the absent Linux runtime subject.'
          printf '%s\n' 'Run scripts/git-town/selftest.sh --integration only in an admitted Worker image.'
