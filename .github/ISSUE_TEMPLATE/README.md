# Issue templates

Templates in this directory enforce eval-first work packets. They collect intent, parentage, path leases, negative controls, artifacts, evidence states, and merge order before implementation.

Do not add a template that allows a task to begin without an owner, parent branch, allowed paths, and at least one negative control.