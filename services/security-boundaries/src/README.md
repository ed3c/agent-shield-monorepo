# Security boundary private source

This directory inherits [`../README.md`](../README.md). Current source validates closed settlement-intent shape and reports named high-risk capabilities as `NOT_IMPLEMENTED`; it owns no native provider or authority.

```text
settlement intent → shape/uniqueness validation → capability-state receipts
```

Provider implementations belong to isolated issues #55–#63 behind #54 contracts. Public composition, adversarial/recovery matrix, status/release, and Human dossier belong to #64. No provider private import or ungrounded state promotion.
